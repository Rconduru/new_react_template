import React from 'react'
import PropTypes from 'prop-types'

class NotFoundPage extends React.Component {
    render () {
        (<div>Essa parada não existe</div>)
    }
}

export default NotFoundPage;
